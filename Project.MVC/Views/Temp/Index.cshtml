@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{

}
@model List<Temp>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">İşlemler</th>

        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    <a class="btn btn-danger" asp-controller="Temp" asp-action="Delete" asp-route-tempId="@item.Id"> Sil</a>
                    <a class="btn btn-info" asp-controller="Temp" asp-action="Update" asp-route-tempId="@item.Id"> Güncelle</a>
                </td>
            </tr>

        }


    </tbody>
</table>